services:
  vue-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cname
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - vue-network

networks:
  vue-network:
    driver: bridge
